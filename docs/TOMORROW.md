# План на 19.08.2026 — довести «Лабораторию Авито» до прода

## Сначала (не пропускать)

1. Проверить утренний автосбор 09:00 МСК (как обычно: Telegram «Авто-сбор 09:00», лог `[auto-publish] done`).
2. **Билд + деплой `/avito`** — главное за сегодня:
   - `cd /var/www/www-root/data/www/proektmap.ru && rm -rf .next && npx next build && pm2 restart proektmap`
   - проверить, что `/avito` открывается: карточки рендерятся, поиск и фильтры по категориям работают.
   - если билд падает — чинить ошибки типов (код уже в `src/app/avito/`: `data.ts`, `page.tsx`, `avito-catalog.tsx`).

## Продолжить «Лабораторию Авито»

3. Сохранить master-list как `docs/AVITO-MASTER-LIST.md` (очередь из 150+ кандидатов на проверку, из заметок Алексея).
4. Проверить `/avito` на реальном телефоне (mobile-first: без горизонтального скролла на 375px).
5. SEO: добавить `/avito` в навигацию (GlobalHeader) и в sitemap; каждой карточке — свой slug для long-tail.
6. Уточнить с Алексеем:
   - расширения Авито (AvToolsPro, LikeStats) — сейчас ссылки ведут на поиск Chrome Web Store, нужны точные URL;
   - когда стартуем фазу 2 (рейтинг авитологов, отзывы, «мой стек») — после появления трафика/аудитории.

## Правила (не забыть)

- После любых правок кода: `rm -rf .next && npx next build && pm2 restart proektmap`.
- Не трогать `/resheniya`, `/graph`, `/universe`, `/solutions` — это другой контур.
- Парсинг/мультиаккаунт помечать бейджем «⚠ риск блокировки», не прятать.
- В конце дня: DEVLOG + `git add -A && git commit && git push`.
