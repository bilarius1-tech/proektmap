# План на сегодня — 17.08.2026 · Mobile-first foundation

> Цель дня: заложить фундамент адаптивности + убрать критичные баги P0.
> Не цель дня: переделать весь сайт и Graph/Universe.

## Принципы
1. Светлая тема — основная. Dark не развиваем сегодня.
2. Сначала система (токены, breakpoints, shell), потом страницы.
3. Каждый этап = правка → скриншот 375px → ок/не ок.
4. Graph / Universe / Admin — не трогаем сегодня.

---

## Этап 0 · Baseline ✅
Скриншоты/замеры 375×812 (light): `/`, `/blog`, `/pricing` — горизонтального overflow нет.
`/corporate-website`, `/auth` — смотрим после foundation.

### Дефекты (приоритет)
1. **Shell:** `.mobile-only` без `display:none` на desktop → гамбургер виден вместе с nav (>960).
2. **Нижние слои:** progress (bottom:0) + FAB (bottom:56) + cookie (bottom:0) конфликтуют; FAB перекрывает CTA «Начать».
3. **Главная:** `minmax(380px)` в виджетах → риск overflow на 375.
4. **Типографика:** на ≤480 `--text-m: 15px` (ниже цели ≥16).
5. **Breakpoints:** дубли 860 / 980 / 1150 рядом с 480 / 768 / 960.
6. **tokens.css:** баг `.btn-ghost:hover { padding-bottom: 56px }`.
7. **Assistant chat:** ширина 340px при `right:20` на узком экране.

**Критерий:** список дефектов с URL — выполнен.

---

## Этап 1 · Mobile foundation
- Один набор breakpoints: 480 / 768 / 960
- `.mobile-only` скрыт на desktop по умолчанию
- Контейнер контента + `overflow-x: clip` на body/main
- Типографика: body ≥16px, заголовки через clamp
- Картинки/таблицы/pre: max-width 100%
- Убрать/ослабить конфликтующие дубли в `globals.css` vs `tokens.css`

**Критерий:** нет горизонтального скролла на `/` и `/blog` при 375px.

---

## Этап 2 · Shell
- Header + MobileMenu: корректный гамбургер ≤960
- Нижняя safe-zone: progress / FAB / assistant / cookie не перекрывают друг друга
- Footer: одна колонка на mobile

**Критерий:** на телефоне можно открыть меню и нажать CTA без борьбы с плавающими кнопками.

---

## Этап 3 · P0-страницы (точечно)
- Главная: `minmax(380px)` → адаптивная сетка
- Architect input: стак на mobile (класс + CSS)
- Blog: уже лучше остальных — добить читаемость статьи
- Blueprint: FAB + sidebar drawer без коллизий

**Критерий:** путь «главная → блог → Blueprint» читается одной рукой.

---

## Этап 4 · Закрытие ✅
- Скриншоты/замеры «после»: 375px без overflow; desktop — hamburger `display:none`, nav `flex`
- Билд + `pm2 restart` — выполнено
- DEVLOG запись — выполнено
- Коммит + push: `a5c76d1e`

---

## Этап 5 · Читаемость статьи на мобильном ✅
- Найдена причина мелкого текста: обёртка статьи наследовала `--text-s` = 14px
- Тело статьи: 18px / 1.75 на ≤768px, 17px на desktop
- Увеличены интервалы абзацев и списков
- Код и таблицы на mobile: не меньше 14px
- Замер 375px: текст 18px, line-height 32.4px, horizontal overflow отсутствует
- Билд + `pm2 restart` — выполнено
- Коммит + push — выполнено

---

## Этап 6 · Заголовок и информация статьи ✅
- Заголовок статьи на mobile: 27–32px вместо 36px, line-height 1.16
- Автор, дата, время чтения, просмотры и комментарии собраны в отдельный блок
- У каждой метрики есть понятная подпись, а не только иконка
- Теги отделены от статистики и прокручиваются одной строкой
- Кнопки «Поделиться» собраны отдельно и получили доступные названия
- Проверка 375px: horizontal overflow отсутствует
- Билд + `pm2 restart` — выполнено
- Коммит + push — выполнено

---

## Вне скоупа сегодня
- Полный отказ от dark theme в коде (только заморозка приоритета)
- Graph / Universe / AI-Land polish
- Admin mobile
- PWA

## Следующий день (черновик)
- Визуальный проход секций главной (hero, квиз, калькулятор)
- Auth / pricing polish
- Упростить header-right на 375 (♡ / тема / Войти)
