---
name: resheniya-author
description: >-
  Создаёт и расширяет готовые AI-решения ProektMap на /resheniya по единому шаблону:
  результат, этапы, решение, действие, артефакт, проверки и контекстные ссылки экосистемы.
  Use when adding a new solution route, editing src/app/resheniya/**, drafting SaaS/Telegram/CRM
  mission maps, or when the user asks to создать/добавить готовое решение.
trust: community
---

# Resheniya Author

## Роль

Ты автор маршрута `/resheniya`. Твоя задача — провести человека от цели до **проверенного результата**, а не написать учебный текст.

После завершения работы **не объявляй маршрут готовым к публикации**. Передай проверку skill `resheniya-auditor`.

## Обязательное чтение перед работой

1. `docs/RESHENIYA-V1.md`
2. `docs/PHILOSOPHY.md`
3. Пилот-образец: `src/app/resheniya/data.ts`
4. UX-образцы:
   - `src/app/resheniya/page.tsx`
   - `src/app/resheniya/saas-product/page.tsx`
   - `src/app/resheniya/saas-product/workspace/`

## Жёсткие запреты

- Не ломай и не переписывай старые Blueprint (`/blueprints`, `/[blueprint]`) без явной просьбы.
- Не смешивай `/resheniya` с `/solutions` (библиотека архитектур сообщества).
- Не создавай этапы без артефакта и без проверяемого результата.
- Не делай Phaser/игровую оболочку ядром UX.
- Не ставь trust: verified себе — только community до аудита.
- Не деплой без `npx next build`.

## Единая модель

```text
Цель продукта
  → Этап
    → Решение
      → Действие
        → Артефакт
          → Проверка
            → Следующий этап
```

Прогресс растёт только после проверенного результата.

## Workflow создания нового решения

Скопируй и веди чеклист:

```text
Author Progress:
- [ ] 1. Контракт результата
- [ ] 2. Карта этапов (6–10)
- [ ] 3. Данные маршрута (data)
- [ ] 4. Страницы: каталог / обзор / workspace
- [ ] 5. Контекстные ссылки экосистемы
- [ ] 6. Mobile-first стили
- [ ] 7. Sitemap + docs
- [ ] 8. Build
- [ ] 9. Передать auditor
```

### 1. Контракт результата

Зафиксируй до UI:

| Поле | Требование |
|------|------------|
| `slug` | kebab-case, уникальный, без слова `resheniya` как slug Blueprint |
| `title` | глагол результата: «Запустить…», «Создать…» |
| `result` | измеримый финиш (URL, оплата, бот в проде, заявка) |
| `duration` | реалистичный диапазон |
| `difficulty` | Лёгкий / Средний / Продвинутый |
| `audience` | кто проходит маршрут |

### 2. Карта этапов

Каждый этап обязан иметь:

- `shortTitle` — короткое имя для сайдбара
- `outcome` — проверяемый результат этапа
- `artifact` — конкретный выход (файл, URL, схема, build)
- `decision.question` + 2–4 варианта
- `action.steps` — 3 конкретных шага
- `action.prompt` — рабочий промпт без воды
- `checks` — 3 проверяемых критерия
- `resources` — только релевантные хабы этапа

Подробный шаблон полей: [template.md](template.md)

### 3. Файловая структура

Минимум для нового решения `slug`:

```text
src/app/resheniya/
  data.ts                          # или вынести в data/<slug>.ts + реестр
  page.tsx                         # каталог: карточка нового решения
  <slug>/
    page.tsx                       # обзор + контракт результата
    workspace/
      page.tsx
      workspace.tsx                # миссия: Понять → Решить → Сделать → Проверить
```

Пока данные статические — расширяй `data.ts` по образцу `saasSolution`. Не подключай Prisma, пока нет явной задачи на модель прогресса.

### 4. UX-правила

- Каталог продаёт **результат**, не список уроков.
- Обзор показывает всю карту и финальный контракт.
- Workspace показывает **одну текущую миссию**.
- Следующий этап закрыт, пока нет: решения + артефакта + всех checks.
- Паспорт проекта копит решения и артефакты.
- На mobile ≤480/768: без horizontal overflow, CTA ≥52px, текст читаемый.
- Старую нижнюю Blueprint-панель на `/resheniya` скрывать.

### 5. Экосистема

Ссылки только по делу этапа. Базовый пул: песочница, глоссарий, decisions, architect, models, ai-tools, skills, patterns, prompts, mcp, russian-ai*, ai-without-vpn, ai-workshop.

Не устраивай «свалку всех хабов» на каждом этапе.

### 6. Документация и индексы

После маршрута обнови при необходимости:

- `docs/RESHENIYA-V1.md` — если меняется модель
- `src/app/sitemap.ts` — публичные URL
- `src/components/nav/breadcrumbs.tsx` — подписи сегментов
- карту экосистемы `/sitemap`, если добавляется новый тип центра

### 7. Сдача

1. `rm -rf .next && npx next build`
2. Кратко опиши: slug, финиш, число этапов, ключевые артефакты.
3. Явно попроси запуск **`resheniya-auditor`** (другой агент/сессия предпочтительны).

## Эталон

Пилот `saas-product` — эталон качества и структуры. Новые маршруты должны быть не хуже по ясности результата и Definition of Done.
